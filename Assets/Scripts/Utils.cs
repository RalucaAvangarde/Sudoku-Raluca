using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Utils : MonoBehaviour
{
    public JsonUtils utils;
    // Start is called before the first frame update
    void Start()
    {
        utils = new JsonUtils();
    }

   
}
